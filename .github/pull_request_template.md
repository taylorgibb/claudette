## What this changes

<!-- and why -->

## Checklist

- [ ] `swift build && swift test` pass
- [ ] Decisions live in `ClaudetteCore` or a presenter, not in a `View`
- [ ] No new free-form property on `AnalyticsEvent`, and nothing new leaves
      the machine without going through `Redactor`
- [ ] Screenshot or recording, if anything visual changed
